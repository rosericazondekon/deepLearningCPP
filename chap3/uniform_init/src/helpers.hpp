#include <vector>
#include <cstddef>

std::vector<float> uniform_initializer(size_t, float, float);
bool chi_squared_test(float, float, size_t, float);
bool t_test(float, float, float, size_t, float);